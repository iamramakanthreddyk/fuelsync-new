/**
 * Readings Module
 * Handles reading creation, attribution (Req #1), and retrieval
 * Exports: ReadingsService
 */

const readingsService = require('./readings.service');

module.exports = {
  readingsService
};
